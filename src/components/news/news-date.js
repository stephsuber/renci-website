import React, { Fragment } from 'react'
import styled from 'styled-components'

export const NewsDate = styled.span(({ theme }) => `
  font-size: 95%;
  font-weight: bold;
  text-transform: uppercase;
  margin: 0;
  line-height: 2.35;
  font-size: 12px;    
`)
